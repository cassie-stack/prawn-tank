import { MutationTree } from 'vuex';
import { ModelsStateInterface } from './state';

const mutation: MutationTree<ModelsStateInterface> = {
  someMutation(/* state: ExampleStateInterface */) {
    // your code
  },
};

export default mutation;
